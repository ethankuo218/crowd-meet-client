import { BundleWordingPipe } from './bundle-wording.pipe';

describe('BundleWordingPipe', () => {
  it('create an instance', () => {
    const pipe = new BundleWordingPipe();
    expect(pipe).toBeTruthy();
  });
});
