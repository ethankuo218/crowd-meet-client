import { NotificationIconPipe } from './notification-icon.pipe';

describe('NotificationIconPipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationIconPipe();
    expect(pipe).toBeTruthy();
  });
});
