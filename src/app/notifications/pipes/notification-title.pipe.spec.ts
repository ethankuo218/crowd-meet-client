import { NotificationTitlePipe } from './notification-title.pipe';

describe('NotificationTitlePipe', () => {
  it('create an instance', () => {
    const pipe = new NotificationTitlePipe();
    expect(pipe).toBeTruthy();
  });
});
